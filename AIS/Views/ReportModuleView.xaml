<UserControl x:Class="AIS.Views.ReportModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AIS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:vm="clr-namespace:AIS.ViewModels" 
             xmlns:conv="clr-namespace:AIS.Converters">
    <UserControl.DataContext>
        <vm:ReportModuleViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <conv:CollectionToStringConverter x:Key="CollectionToStringConverter"/>
    </UserControl.Resources>
    <Grid>
        <DataGrid ItemsSource="{Binding Reports}"  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="IdGreenHouse" Binding="{Binding IdGreenHouse}"/>
                <DataGridTextColumn Header="Time" Binding="{Binding Time}"/>
                <DataGridTextColumn Header="Temperature" Binding="{Binding Temperature}"/>
                <DataGridTextColumn Header="CO2" Binding="{Binding CO2}"/>
                <DataGridTextColumn Header="Humidity" Binding="{Binding Humidity}"/>
                <DataGridTextColumn Header="Commands" Binding="{Binding Commands, Converter={StaticResource CollectionToStringConverter}}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
