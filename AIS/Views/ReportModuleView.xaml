<UserControl x:Class="AIS.Views.ReportModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AIS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:vm="clr-namespace:AIS.ViewModels" 
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             xmlns:conv="clr-namespace:AIS.Converters" d:DataContext="{d:DesignInstance Type=vm:ReportModuleViewModel}">
    <UserControl.Resources>
        <conv:CollectionToStringConverter x:Key="CollectionToStringConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height = "1*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Reports}"  AutoGenerateColumns="False" Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}"></DataGridTextColumn>
                <DataGridTextColumn Header="IdGreenHouse" Binding="{Binding IdGreenHouse}"/>
                <DataGridTextColumn Header="co2_value" Binding="{Binding CO2Value}"/>
                <DataGridTextColumn Header="humidity_value" Binding="{Binding HumidityValue}"/>
                <DataGridTextColumn Header="temperature_value" Binding="{Binding TemperatureValue}"/>
                <DataGridTextColumn Header="co2_pred" Binding="{Binding CO2Pred}"/>
                <DataGridTextColumn Header="humidity_pred" Binding="{Binding HumidityPred}"/>
                <DataGridTextColumn Header="temperature_pred" Binding="{Binding TemperaturePred}"/>
                <DataGridTextColumn Header="command_co2" Binding="{Binding commandCO2}"/>
                <DataGridTextColumn Header="command_humidity" Binding="{Binding commandHumidity}"/>
                <DataGridTextColumn Header="command_temperature" Binding="{Binding commandTemperature}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="1" Command="{Binding GetExcelAsyncCommand}">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width = "1*"/>
                    <ColumnDefinition Width = "auto"/>
                </Grid.ColumnDefinitions>
                <svg:SvgViewbox Source="/Resources/excel.svg" Grid.Column="0" MaxHeight="40" MaxWidth="40"/>
                <Label Content="Выгрузить в EXCEL" Grid.Column="1" VerticalContentAlignment="Center" FontSize="18"/>
            </Grid>
        </Button>
    </Grid>
</UserControl>
