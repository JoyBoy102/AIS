<UserControl x:Class="AIS.Views.MonitoringModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AIS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:vm="clr-namespace:AIS.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:MonitoringModuleViewModel}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <DataTemplate x:Key="ListBoxDataTemplate">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center"></TextBlock>
                    <ToggleButton Grid.Row="0" Grid.Column="1" Style="{StaticResource ToggleButtonArrow}" 
                                  IsChecked="{Binding ToggleButtonState}" 
                                  Command="{Binding DataContext.ShowSensorsTableCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" 
                                  CommandParameter="{Binding}"
                                  HorizontalAlignment="Right"></ToggleButton>
                    <DataGrid Grid.Row ="1" Grid.ColumnSpan="2" ItemsSource="{Binding SensorsReadings}"  AutoGenerateColumns="False" Visibility="{Binding ToggleButtonState, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ID}"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                            <DataGridTextColumn Header="Value" Binding="{Binding Value}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ListBox ItemsSource="{Binding GreenhouseItems}" ItemTemplate="{StaticResource ListBoxDataTemplate}" Margin="20"></ListBox>
    </Grid>
</UserControl>
