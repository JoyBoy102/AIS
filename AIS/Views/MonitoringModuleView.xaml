<UserControl x:Class="AIS.Views.MonitoringModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:AIS.Views"
             xmlns:s="clr-namespace:AIS.Structs"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:AIS.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:MonitoringModuleViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <DataTemplate DataType="{x:Type s:SensorsReadingCollection}">
            <ui:DataGrid ItemsSource="{Binding}"
                         CanUserAddRows="False"
                         IsReadOnly="True" CanUserResizeColumns="False"
                         VerticalScrollBarVisibility="Disabled"
                         AutoGenerateColumns="False">
                <ui:DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding SensorId}"/>
                    <DataGridTextColumn Header="Тип" Binding="{Binding Type, Converter={StaticResource ResourceKey=EngToRus}}"/>
                    <DataGridTextColumn Header="Значение" Binding="{Binding Value}"/>
                    <DataGridTextColumn Header="Мощность исполнительного устройства" Binding="{Binding CurrentPower}"/>
                </ui:DataGrid.Columns>
            </ui:DataGrid>
        </DataTemplate>
    </UserControl.Resources>

    <ListBox ItemsSource="{Binding GreenhouseItems}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <ui:CardExpander Header="{Binding Name}" Content="{Binding SensorsReadingCollection}"/>
            </DataTemplate>
        </ListBox.ItemTemplate>
        <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem" BasedOn="{StaticResource DefaultListBoxItemStyle}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                            <Border x:Name="Border"
                                    Margin="0"
                                    Padding="{TemplateBinding Padding}"
                                    Background="Transparent"
                                    BorderThickness="1"
                                    CornerRadius="{TemplateBinding Border.CornerRadius}">
                                <ContentPresenter />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource MenuBarItemBackgroundSelected}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource ListBoxItemSelectedForegroundThemeBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ListBox.ItemContainerStyle>
    </ListBox>
</UserControl>