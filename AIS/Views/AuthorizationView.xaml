<Window x:Class="AIS.Views.AuthorizationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AIS.Views" xmlns:viewmodels="clr-namespace:AIS.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:AuthorizationViewModel}"
        mc:Ignorable="d"
        Title="AuthorizationView" Height="450" Width="400">
    <Grid>
        <Border Background="LightGray" CornerRadius="20" Margin="50 50">
            <StackPanel>
                <TextBlock Text="Войти в учетную запись"
                           HorizontalAlignment="Center"
                           Margin="20"
                           FontSize="16"
                           FontWeight="Bold"></TextBlock>
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="Gray" BorderBrush="Black" BorderThickness="1" Margin="5,5,2.5,5" CornerRadius="8">
                        <Button Content="Почта" HorizontalAlignment="Stretch" Style="{StaticResource BorderButtonStyle}" Command="{Binding EmailModeSelectCommand}"/>
                    </Border>
                    <Border Grid.Column="1" Background="Gray" BorderBrush="Black" BorderThickness="1" Margin="2.5,5,5,5" CornerRadius="8">
                        <Button Content="Телефон" HorizontalAlignment="Stretch" Style="{StaticResource BorderButtonStyle}" Command="{Binding NumberModeSelectCommand}"/>
                    </Border>
                </Grid>

                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition></RowDefinition>
                        <RowDefinition></RowDefinition>
                        <RowDefinition></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" BorderBrush="Black" BorderThickness="1" CornerRadius="8" Margin="10">
                        <TextBox BorderThickness="0" Background="Transparent" Padding="5" Text="{Binding FirstTextBoxData}" Foreground="Gray"/>
                    </Border>

                    <Border Grid.Row="1" Background="White" BorderBrush="Black" BorderThickness="1" CornerRadius="8" Margin="10">
                        <TextBox BorderThickness="0" Background="Transparent" Padding="5" Text="{Binding Password}" Foreground="Gray"/>
                    </Border>

                    <Border Grid.Row="2" Background="Gray" BorderBrush="Black" BorderThickness="1" CornerRadius="8" Margin="10">
                        <Button Content="Войти" HorizontalAlignment="Stretch" Style="{StaticResource BorderButtonStyle}" Command="{Binding SignInCommand}"/>
                    </Border>

                    <Border Grid.Row="3" Background="Gray" BorderBrush="Black" BorderThickness="1" Margin="10" CornerRadius="8">
                        <Button Content="Регистрация" HorizontalAlignment="Stretch" Style="{StaticResource BorderButtonStyle}" Command="{Binding RegistrationCommand}"/>
                    </Border>
                </Grid>

            </StackPanel>
            
        </Border>
    </Grid>
</Window>
