<UserControl x:Class="AIS.Views.CRUDView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AIS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:vm="clr-namespace:AIS.ViewModels">
    <Grid>
        <TabControl>
            <TabItem Header="Теплицы">
                <DataGrid ItemsSource="{Binding Greenhouses}" IsReadOnly="True" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID теплицы" Binding="{Binding ID}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Название теплицы" Binding="{Binding Name}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Местоположение теплицы" Binding="{Binding Location}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Описание теплицы" Binding="{Binding Description}"></DataGridTextColumn>
                        <DataGridTemplateColumn Header="Удалить">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="❌" 
                                            Command="{Binding DataContext.DeleteRowCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding ID}"
                                            Width="30" Height="30"
                                            FontSize="14"
                                            Background="Transparent" BorderThickness="0"
                                            Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Редактировать">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="✏️"
                                            
                                            CommandParameter="{Binding ID}"
                                            Width="30" Height="30"
                                            FontSize="14"
                                            Background="Transparent" BorderThickness="0"
                                            Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Датчики">
                <DataGrid ItemsSource="{Binding Sensors}" IsReadOnly="True" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID датчика" Binding="{Binding ID}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Тип датчика" Binding="{Binding Type}"></DataGridTextColumn>
                        <DataGridTextColumn Header="ID теплицы" Binding="{Binding GreenhouseID}"></DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
