<UserControl x:Class="AIS.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AIS.Views" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:AIS.ViewModels"
             xmlns:conv="clr-namespace:AIS.Converters"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:ThemeToIndexConverter x:Key="ThemeToIndexConverter"/>
    </UserControl.Resources>
    <StackPanel Margin="0,0,0,24">
        <ui:TextBlock
        Margin="0,0,0,8"
        FontTypography="Subtitle"
        Text="Настройка приложения" />
        <ui:CardControl Margin="0,0,0,12" Icon="{ui:SymbolIcon Color24}">
            <ui:CardControl.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ui:TextBlock
                    Grid.Row="0"
                    FontTypography="Body"
                    Text="Тема приложения" />
                    <ui:TextBlock
                    Grid.Row="1"
                    Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                    Text="Выберите тему приложения для отображения" />
                </Grid>
            </ui:CardControl.Header>
            <ComboBox
            Grid.Column="1"
            MinWidth="200"
            
            SelectedIndex="{Binding CurrentApplicationTheme, Converter={StaticResource ThemeToIndexConverter}, Mode=TwoWay}">
                <ComboBoxItem Content="Light" />
                <ComboBoxItem Content="Dark" />
                <ComboBoxItem Content="High Contrast" />
            </ComboBox>
        </ui:CardControl>

        <ui:CardControl Margin="0,0,0,12" Icon="{ui:SymbolIcon FlagClock32}">
            <ui:CardControl.Header>
                <ui:TextBlock
                    Grid.Row="0"
                    FontTypography="Body"
                    Text="Вкл/Выкл автоматический режим"
                    VerticalAlignment="Center"/>
            </ui:CardControl.Header>
            <ui:ToggleSwitch IsChecked="{Binding AutoMode, Mode=TwoWay}"></ui:ToggleSwitch>
        </ui:CardControl>
    </StackPanel>
</UserControl>
